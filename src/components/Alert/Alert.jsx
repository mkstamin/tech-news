/* eslint-disable jsx-a11y/accessible-emoji */
import React from 'react';
import Container from './Alert.stles';

const Alert = () => (
    <Container>
        <p>Working on change them.....(click again the Button to hide this text 😊)</p>
    </Container>
);

export default Alert;
